# Render Blueprint configuration for VEEGA RAVE Static Site
services:
  - type: web
    name: vijag-veega-rave
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
